<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Registrasi Akun | Booking Futsal</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background: linear-gradient(135deg, #43d8c9 0%, #283655 100%);
      min-height: 100vh;
      margin: 0;
      font-family: 'Poppins', Arial, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .register-box {
      background: #fff;
      padding: 42px 35px 32px 35px;
      border-radius: 22px;
      box-shadow: 0 8px 36px #43d8c966, 0 1.5px 12px #28365533;
      max-width: 370px;
      width: 100%;
      margin: 38px 0;
      position: relative;
      animation: fadeInUp 0.7s;
    }
    .reg-title {
      text-align: center;
      color: #43d8c9;
      font-weight: 800;
      font-size: 2rem;
      margin-bottom: 20px;
      letter-spacing: 2px;
    }
    .register-box label {
      color: #283655;
      font-weight: 600;
      margin-bottom: 7px;
      display: block;
      letter-spacing: .2px;
    }
    .register-box input {
      width: 100%;
      border-radius: 10px;
      border: 2px solid #e3e9ff;
      padding: 12px 13px;
      font-size: 1.07rem;
      background: #f3f7fd;
      margin-bottom: 19px;
      color: #23243c;
      transition: border 0.2s;
    }
    .register-box input:focus {
      border-color: #43d8c9;
      outline: none;
    }
    .btn-register {
      width: 100%;
      padding: 13px 0;
      background: linear-gradient(90deg, #43d8c9 40%, #283655 100%);
      color: #fff;
      font-size: 1.1rem;
      font-weight: 700;
      border: none;
      border-radius: 13px;
      box-shadow: 0 4px 16px #43d8c950;
      cursor: pointer;
      letter-spacing: 1px;
      margin-bottom: 8px;
      transition: background 0.21s, box-shadow 0.18s, transform 0.17s;
    }
    .btn-register:hover {
      background: linear-gradient(90deg, #283655 40%, #43d8c9 100%);
      box-shadow: 0 8px 26px #43d8c968;
      transform: scale(1.04) translateY(-2px);
    }
    .login-link {
      display: block;
      text-align: center;
      color: #283655;
      margin-top: 14px;
      font-size: .99rem;
      text-decoration: none;
    }
    .login-link:hover {
      color: #43d8c9;
      text-decoration: underline;
    }
    .msg-error {
      background: #ffbcbc;
      color: #b42020;
      border-radius: 8px;
      padding: 9px 12px;
      text-align: center;
      margin-bottom: 17px;
      font-size: 1em;
    }
    .reg-back {
      position: absolute; left: 19px; top: 17px;
      color: #43d8c9;
      font-weight: 700;
      text-decoration: none;
      font-size: 1.22em;
      transition: color .16s;
    }
    .reg-back:hover { color: #283655; }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(18px);}
      to { opacity: 1; transform: translateY(0);}
    }
  </style>
  <script>
    function cekForm() {
      let username = document.getElementById('username').value.trim();
      let password = document.getElementById('password').value;
      let ulang = document.getElementById('ulang').value;
      if (username.length < 4) {
        alert('Username minimal 4 karakter!');
        return false;
      }
      if (password.length < 4) {
        alert('Password minimal 4 karakter!');
        return false;
      }
      if (password !== ulang) {
        alert('Konfirmasi password tidak sama!');
        return false;
      }
      return true;
    }
  </script>
</head>
<body>
  <form class="register-box" action="/register" method="POST" autocomplete="off" onsubmit="return cekForm()">
    <a class="reg-back" href="/login" title="Kembali ke login">&#8592; Login</a>
    <div class="reg-title">Daftar Akun</div>
    <% if (error) { %>
      <div class="msg-error"><%= error %></div>
    <% } %>
    <div>
      <label>Nama Lengkap</label>
      <input type="text" name="nama" placeholder="Nama kamu" required autocomplete="off" maxlength="32">
    </div>
    <div>
      <label>Username</label>
      <input type="text" name="username" id="username" placeholder="Username unik" required autocomplete="off" minlength="4" maxlength="20">
    </div>
    <div>
      <label>Password</label>
      <input type="password" name="password" id="password" placeholder="Minimal 4 karakter" required minlength="4" maxlength="32" autocomplete="new-password">
    </div>
    <div>
      <label>Ulangi Password</label>
      <input type="password" id="ulang" placeholder="Ulangi password" required minlength="4" maxlength="32" autocomplete="new-password">
    </div>
    <button class="btn-register" type="submit">Daftar Sekarang</button>
    <a href="/login" class="login-link">Sudah punya akun? Login disini</a>
  </form>
</body>
</html>
